# JWT secret (change in production)
SECRET_KEY=change-me

# Access token lifetime (minutes)
ACCESS_TOKEN_EXPIRE_MINUTES=120

# CORS allow origins (comma-separated) or "*"
CORS_ALLOW_ORIGINS=*

# Data directory for JSON stores
DATA_DIR=data
